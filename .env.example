# MedLens Mobile — Environment Configuration
# Copy to .env and fill in your values:  cp .env.example .env

# Backend URLs
BACKEND_WS_URL=wss://YOUR-CLOUD-RUN-URL
BACKEND_HTTP_URL=https://YOUR-CLOUD-RUN-URL
